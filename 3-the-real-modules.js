// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum )

const modules = require('./4-modules');
const sayHelloWorld = require('./5-utils')
const alternatives = require('./6-alternatives')

// sayHelloWorld(modules.peter)
// sayHelloWorld(modules.susan)

// console.log(alternatives)

require('./7-mind-boggling')